# EmbyMedia 安全

[English](SAFETY.md) | 中文

## 概要

EmbyMedia 能够修改外部媒体服务。身份验证、目标校验、持久结果、备份与运维检查分别处理不同风险，彼此不能替代。

## 目录

- [凭据与访问](#凭据与访问)
- [破坏性工作](#破坏性工作)
- [状态与恢复](#状态与恢复)

## 凭据与访问

115 Cookie、Emby key、CloudDrive token、webhook secret、Agent token 明文、浏览器用户数据库及恢复密码不得进入提交与日志。每个 Agent 安装使用单独命名的令牌；共享令牌无法区分调用方。浏览器与 provider 会话均按 secret 处理。

后端监听保持位于登录代理后方的回环地址。明文 HTTP 不加密凭据；网络不可信时，使用受保护网络或另行配置 TLS。公开 Agent 请求不能自行提供浏览器身份，明确无效的凭据必须拒绝。

## 破坏性工作

浏览器删除要求危险操作设置与确认。MCP 删除将准确目标绑定到请求，要求十五分钟内由已登录浏览器批准一次，并只消费一次审批。Agent token 不能授予该审批、启用设置或调用浏览器 REST 删除路由。Emby 原生删除要求获授权的 Emby 管理员设备会话，而非应用 API key。

明确启用的完结整包替换任务，只有在危险操作启用且验证完整替代副本后才能删除旧根。不能将普通读写 scope 理解为绕过这些检查的权限。原始媒体身份与生成 STRM 身份必须分别重新验证。

任务入队不等于操作完成。读取终态结果与错误。重启或部分完成结果可能留下外部已接受的副作用；重试前检查 provider 状态，不要盲目重放。发现工具只证明注册存在。

## 状态与恢复

仓库清理绝不能删除数据库、WAL/SHM 文件、凭据、被忽略的本地状态或原始媒体。保持生产 SQLite 格式与数据库独占锁。STRM 输出需要共享 Emby ACL；不要通过削弱原始媒体权限修复输出访问。

使用[运维指南](docs/operations.zh.md)中的在线备份与隔离还原流程。运行中数据库的原始文件副本不是一致备份，还原目录也不构成覆盖生产的授权。恢复 secret 与独立媒体备份应保存在源码仓库之外。
