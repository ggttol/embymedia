{
	admin 127.0.0.1:2019
}

# Shared host configuration. Each application owns only its files below these
# directories; release installers must not replace this root file.
import /etc/caddy/snippets/*.caddy

http://gaotao.cc {
	import /etc/caddy/routes/*.caddy

	handle {
		respond 404
	}
}

import /etc/caddy/sites/*.caddy
