#!/bin/sh
set -eu
for variable in CLOUDDRIVE_IMAGE EMBY_IMAGE AUTHELIA_IMAGE; do
  eval "value=\${$variable:-}"
  case "$value" in
    *@sha256:????????????????????????????????????????????????????????????????) ;;
    sha256:????????????????????????????????????????????????????????????????) ;;
    *) echo "$variable must be an immutable SHA-256 image reference" >&2; exit 1 ;;
  esac
done
