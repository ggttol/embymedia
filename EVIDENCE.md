# EVIDENCE.md

| Claim | Source | Status | Allowed wording |
| --- | --- | --- | --- |
| Emby and CloudDrive2 are healthy Docker services on Debian | Live container health checks after the CloudDrive restart incident | verified | “服务健康” at the recorded check time |
| Emby sees the CloudDrive media mount | `/media/.embymedia-health-canary` read inside the Emby container | verified after Emby restart | “媒体挂载已重新接入”；do not claim a user playback succeeded without a playback check |
| The V2 runtime needs DSH, Node, or PostgreSQL | systemd/Compose cutover and standalone build | false | “生产 runtime 不依赖 DSH、Node 或 PostgreSQL” |
| All thirty-four MCP handlers reach a service or durable operation | MCP registry, provider, task, schedule, storage, authorization, and approval-flow tests | verified locally | “34 / 34 tools registered with real implementation paths”; provider availability remains runtime evidence |
| 115 list, mkdir, rename, move, recycle, share receive/create, and offline download call provider endpoints | `TestDriveProviderOperations` | verified against protocol-compatible HTTP fixtures | Name provider configuration as a prerequisite |
| CloudDrive status and remount use official gRPC methods | generated version-matched subset plus `TestCloudDriveGRPCHealthAndRemount` | verified against a gRPC provider fixture | “gRPC 实现已验证”；live remount was not repeated after the playback incident |
| STRM synchronization rejects output symlinks and escaping targets | Media service containment tests | verified | “STRM 写入和验证限制在配置根目录内” |
| Persistent tasks support cancellation, attempts, logs, restart recovery, and explicit retry | task queue/storage tests | verified | Only `completed` is success |
| REST and HTTP MCP reject unauthenticated Agent traffic and enforce scopes/rates | API, security, Streamable MCP, and HTTP-wrapper tests | verified | Browser login and Agent token are distinct paths |
| Agent audit records redact nested credentials and contain bounded valid JSON | security and MCP policy tests | verified | “输入和输出摘要脱敏并受限” |
| Debian Hermes is version 0.21.0 and discovers the standalone registry | `hermes mcp test embymedia` on gaotao.cc | verified before final deployment refresh | Re-run after deployment before final claim |
| The V2 Hermes skill is enabled and stale DSH skills are absent | `hermes skills list --source local --enabled-only` | verified | “embymedia-v2-operator 已启用” |
| Streamable HTTP, stdio, and legacy SSE transports answer their protocol entry paths | real local binary probes | verified | Legacy SSE remains loopback-only |
| OpenAPI contains every registered API route with unique operation IDs | route-coverage test and Redocly validation | verified | State the observed path count only after final build |
| Automatic TMDB candidate selection is safe | no deterministic disambiguation source | false | Require explicit TMDB ID; do not claim automatic matching |
| Browser user administration preserves password secrecy and invalidates versioned sessions after password changes | Login state unit tests, HTTP lifecycle smoke, and deployed administrator API exercise | verified | “管理员可管理浏览器用户”；do not claim operators have restricted media operations |
| Login, protected-route redirect, user roster, and logout work at desktop and mobile widths | Browser smoke through the local authentication proxy plus deployed login and protected-route checks | verified | “登录与退出流程可用” |
| Public HTTP clipboard support | Browser inspection on `http://gaotao.cc:3080` reports an insecure context and no Clipboard API | observed | Offer manual copying; never claim automatic copy succeeded without completion |
| Browser workflow improvements | Local Go/login/Vite browser exercises: real token lifecycle and task failure logs; intercepted search pagination, transfer targets, and file permission failures; 375/820/1440 px route review | verified within stated scope | Fixture-based file checks do not prove a live 115 write; local verification does not mean production deployment |
| Previous production MCP execution | Direct stdio `tools/call` against the earlier eighteen-tool deployment, 2026-09-05 | 17 tools succeeded against live providers; the user explicitly skipped `cd2_remount` during active Apple TV playback | Historical evidence does not verify the sixteen newly added tools or the expanded deployment |
| Expanded production Agent capability | Deployed `tools/list`, direct stdio calls, browser approval UI, and Hermes probe on release `20260905T164138Z-autonomy` | 34 tools discovered; all 16 new tools exercised; 15 completed their intended non-destructive/request/schedule/config behavior; `c115_execute_delete` refused an unapproved and then rejected request; `cd2_remount` refused active playback | “34 tools available”; distinguish successful operations from expected safety refusals and do not claim a media deletion was executed |
| Automatic episode completion refreshes each TMDB-bound Series' Emby metadata before the missing-episode scan so TMDB episodes published after Emby's cached season snapshot become visible; refresh failures log and continue | Refresh-ordering regression plus the full auto-fill suite; production incident: 交锋 E21/E22 aired 2026-09-15 but Emby's cached season data stopped at E20, hourly runs reported no gaps, and manual refreshes were throttled until a forced same-day transfer completed the two episodes | verified: production transfers 4f869ba5 / 2f9ba85d landed E21/E22 and Emby reached 22 episodes; Emby provider throttling may still delay detection by one cycle | Report metadata-refresh counts from task logs; do not claim zero-delay detection while Emby throttles |
| Scheduled poster repair requests full Emby image refreshes, downloads only unique remote poster candidates when refresh remains missing, and distinguishes confirmed repairs from remaining or failed items | `TestTaskQueueRepairsAndVerifiesMissingPoster`, `TestPosterRepairDownloadsUniqueRemoteCandidate`, paged missing-poster scan, cancellation tests, and production executions through `c793ab9b-9801-4a78-a366-07e073cd8310` | verified against protocol-compatible Emby fixtures; production reduced missing posters from 73 to 0, including one exact manual candidate and one generated archive title card for items with no safe remote candidate | Report requested, candidate-downloaded, repaired, failed, remaining, and timeout state separately |
| Metadata repair applies only unique high-confidence title/year/type TMDB candidates and defers duplicate groups | query-derivation, candidate scoring, provider-field decoding, unique-match application, duplicate-group tests, and production executions `05fe7dad-efde-4a1a-b1db-8fc702105d30` / `1ba1fddd-df75-491d-92da-14955c0a2b06` | verified against protocol-compatible Emby fixtures; production reduced missing TMDB identities from 79 to 38 and enabled the poster workflow to reach zero missing posters | “安全自动匹配” means the enforced confidence and uniqueness rules; ambiguous results remain review items |
| EmbyMedia product version is 2.1.0 across OpenAPI, MCP, Web package metadata, browser diagnostics, and release documentation | shared Go `product.Version`, browser version module, protocol tests, and production build | verified by focused API/MCP tests and built UI inspection | Use `2.1.0` for protocols and releases; compact UI may display `V2.1` |
| Release installation does not cancel active background work by default and supports an explicit immediate override | deployment harness plus production execution `d23926e7-41b1-4f2a-a2f8-82c31a4b7355` | verified: default installation waited for completion before stopping; `EMBYMEDIA_DEPLOY_FORCE=1` activated the parallel release without waiting as explicitly requested | State the one-hour default and that force mode cancels the running execution |
| Hourly Emby refresh skips unchanged STRM source traversal while daily full synchronization retains reconciliation and target verification; full source scans report estimated progress every 500 media files | Fast-refresh queue regression, media service race and collision tests, production full task `0f47d1b6-d2c1-4026-a15e-d07a76565b9f`, and production fast tasks `545b0a72-0856-4c0d-8146-ec590ac04f79` / `b314dbff-33aa-4c71-8626-98bc0f0a05b9` | verified: the 29,867-file full baseline took 25m44s; tracked fast scans completed in 17.5s and 90.0s, with `strm_sync=skipped`; a two-way concurrent FUSE source walk was rejected after 6,000 files took seven minutes | Report the observed 17.5–90.0s range, not a guaranteed duration; CloudDrive webhooks are disabled, so the daily full synchronization must remain enabled |
| Pending work resumes in FIFO order, schedule updates return persisted timing, and unchanged deployment configuration emits no false CloudDrive ingestion | Storage/service/deployment regressions plus production schedule reads and schedule-linked fast execution `b314dbff-33aa-4c71-8626-98bc0f0a05b9` | verified locally and deployed; episode completion runs hourly at minute 15, fast Emby refresh hourly at minute 45 with `sync_strm=false`, daily full STRM synchronization and tracked Emby refresh at 02:00, metadata repair at 03:30, and poster repair at 04:15 | Distinguish the hourly fast scan from the daily full reconciliation; the daily full task is the fallback while CloudDrive webhooks remain disabled |
| Deleting a 115 title removes its empty generated STRM directory before the next Emby scan | stale reconciliation regression, production item `213360`, and production task `b29dbe91-a552-44da-bb5d-080d7ebb6adc` | verified: exact ghost directory removed; Emby exact search returned zero items; smoke task removed one stale STRM and its one empty directory while retaining 70 valid targets | Report disappearance only after both filesystem and Emby checks pass |
| Canonical media classification is deployed across 115, STRM, and Emby | Frozen production artifacts in `/srv/embymedia/backups/media-normalization-final-20260909T044742Z`, live 115 root listing, normalized STRM manifest, Emby API verification, and production home inspection | verified: the 115 root has exactly the eleven canonical media directories plus its health canary; `/strm-v2` has 23,830 STRM files and no other files; Emby has the same eleven source libraries plus `合集`, every planned provider identity, alternate version, and episode number passed verification, 566 destination user-state records matched after migration, and the obsolete 28,756-file STRM tree is quarantined with no runtime mount | Report provider, plan, STRM, Emby, and user-state counts separately; `_待回收` and the pre-cutover STRM tree are quarantine, not permanent deletion |
| Animation and child curation use separate canonical libraries | Reviewed SQLite plan and production Emby verification | verified: `动漫` contains 31 Series identities and 1,998 episode numbers, `动漫电影` contains 156 Movie identities across 178 versions, and `儿童` contains 367 Movie identities across 376 versions | Preserve explicit child curation before applying general animation classification; do not infer audience suitability from the Animation genre alone |
| 115 directory listing can throttle independently of valid mutation credentials | Production normalization runs and `TestDriveListFallsBackFromMethodNotAllowed` | observed: high request concurrency caused HTTP 405 from both listing hosts while completed plan rows remained resumable; the alternate APS listing endpoint succeeds when the Web API listing endpoint alone rejects the session | Say that the executor stops at its last completed target group and requires a refreshed Cookie; do not describe HTTP 405 as a failed move or lost operation |
| Direct-connected NAS transfer can complete a full Quark share without proxy traffic | Production execution `eec0ee7c-1f82-48a8-a0ff-fcd5bd4cb8a1`, NAS checkpoints, and 115 API identity checks | verified live: 33/33 files, 34.64 GB, zero failed items | “NAS 直连传输并逐文件核对完成”; do not imply cataloging or Emby ingest |
| Renaming a CloudDrive2 FUSE file while its asynchronous 115 upload is active is unsafe | Side-by-side 1 GB live probes and production 2.9 GB episode recovery | verified: rename left `<name>**..uploading`; direct final-name write settled; production retry advanced after rewrite | “直接写入最终名称并由 115 核对”; never advertise FUSE rename as atomic publication |
| Verified manual Quark imports can continue through move, canonical naming, STRM synchronization, Emby scan, and episode verification | Production source `eec0ee7c-1f82-48a8-a0ff-fcd5bd4cb8a1`, ingest execution `cd571080-c9dd-43e1-b537-d1eefaeb25a7`, live 115 source/destination listings, and Emby search | verified live: all 33 files moved from `_待整理` into `电视剧追更/特别行动：母狮 (2023) [tmdbid=113962]`; 16 imported episode numbers across seasons 1–2 passed post-scan verification; zero findings | “夸克分享全自动入库完成” only after the grouped ingest task reports `stage=verified`; ambiguous imports must remain “待确认” |

No third-party logo, customer, price, performance guarantee, successful playback observation, or unsupported capability may be added.
